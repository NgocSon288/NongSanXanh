const urlStr = 'https://localhost:5001/api';

const LOCAL_STORAGE_TOKEN_NAME = 'LOCAL_STORAGE_TOKEN_NAME';

const ROLES = {
	anonymous: 'anonymous',
	member: 'member',
	provider: 'provider',
	admin: 'admin',
};

export { urlStr, LOCAL_STORAGE_TOKEN_NAME, ROLES };
